# Enemies

Enemy scenes and enemy-local components belong here.

The first moving enemy is defined in `slime.tscn`; its short-range direct pursuit is implemented in `slime.gd`.

`boss_slime.tscn` reuses the Slime scene with boss health, scale, movement, and contact-damage tuning.
